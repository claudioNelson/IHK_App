// Laedt die Styles der Pruefungsseiten (Praefix ex-) fuer die Uebersicht.
import "./pruefung.css";
import type { ReactNode } from "react";

export default function PruefungenLayout({ children }: { children: ReactNode }) {
  return children;
}
